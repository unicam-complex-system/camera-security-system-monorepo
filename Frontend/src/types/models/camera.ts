/** This is a data type that denotes a camera. Camera is composed of
 * properties such as key, name, isActive, etc.
 * */
export type Camera = {
  id: string;
  name: string;
  isActive: boolean;
  url: string;
};
