/** This is a data type that denotes a user who uses the system.
 * It is mainly composed of firstName, lastName, email, etc.
 * */
export interface User {
  firstName: string;
  lastName: string;
  email: string;
  mobileNumber: string;
}
